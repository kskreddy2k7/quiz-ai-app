name: Build Android APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-apk:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Set up Java 17
        uses: actions/setup-python@v5
        with:
          python-version: "3.10" # Placeholder for setup-python, redundant but safe

      - name: Install system dependencies
        run: |
          sudo apt update
          sudo apt install -y \
            build-essential \
            git \
            python3-dev \
            zip \
            unzip \
            openjdk-17-jdk \
            libgl1-mesa-dev \
            libgles2-mesa-dev \
            zlib1g-dev \
            autoconf \
            automake \
            libtool \
            pkg-config \
            libncurses5-dev \
            libncursesw5-dev \
            libtinfo5 \
            cmake \
            libffi-dev \
            libssl-dev \
            ant \
            ccache

      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.buildozer
            .buildozer
            ~/.gradle
            ~/.android
          key: ${{ runner.os }}-build-cache-${{ hashFiles('buildozer.spec', 'requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-build-cache-

      - name: Install Buildozer and tools
        run: |
          pip install --upgrade pip
          pip install "cython<3.0" "setuptools<70" "wheel" "buildozer==1.5.0" "sh"

      - name: Build APK (DEBUG)
        env:
          ANDROID_API: 33
          ANDROID_MINAPI: 21
          P4A_BUILD_ISOLATED: 0
        run: yes | buildozer -v android debug

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: quiz-ai-app-apk
          path: bin/*.apk
          if-no-files-found: error
