# quiz-ai-app

## AI setup (optional)
Set `OPENAI_API_KEY` to enable AI-generated questions. If not set, the app falls back to the local generator.

Optional: set `OPENAI_MODEL` (default: `gpt-4o-mini`).
